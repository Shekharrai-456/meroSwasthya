// No seed data yet - Phase 0 (foundation) has no entities to seed. Real seed
// data (REQ-SEED-001..003: facilities, invite codes, demo users/patients,
// code lists) is added here as each owning module lands, per
// docs/PROJECT_PLAN.md's phase order. This script exists now so
// `make seed`/`npm run seed`/`npm run demo:reset` are wired up and runnable
// from the very first commit, per the Session 2 brief.

console.log('No seed data defined yet (Phase 0). Nothing to do.');
